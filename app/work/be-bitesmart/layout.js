export const metadata = {
  "title": "Be BiteSmart — Parent-first digital strategy",
  "description": "A parent-first digital strategy for Be BiteSmart, reorganising child-safety education around real parent needs, user journeys, content systems and governance."
};

export default function WorkLayout({ children }) {
  return children;
}
